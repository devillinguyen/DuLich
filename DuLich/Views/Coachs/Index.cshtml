@model IEnumerable<DuLich.Models.Coach>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create a new Coach", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayName("Seat")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Seat.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "", new { id=item.Id }) |
            @Html.ActionLink("Details", "", new { id=item.Id }) |
            @Html.ActionLink("Delete", "", new { id=item.Id })
        </td>
    </tr>
}

</table>
